name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    name: CI & Release
    permissions:
      contents: write
    uses: sisques-labs/workflows/.github/workflows/trunk-maven-release.yml@main
    secrets: inherit
